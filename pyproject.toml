[project]
name = "rag-chunking-benchmark"
version = "0.1.0"
description = "A Streamlit app for benchmarking RAG chunking strategies."
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
